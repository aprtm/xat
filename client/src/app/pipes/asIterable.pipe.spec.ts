import { IterateObjectPipe } from './iterate-object.pipe';

describe('IterateObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new IterateObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
