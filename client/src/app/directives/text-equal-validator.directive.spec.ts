import { TextEqualValidatorDirective } from './text-equal-validator.directive';

describe('TextNotEqualValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEqualValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
